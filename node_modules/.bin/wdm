#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webdriver-manager-replacement/bin/webdriver-manager-replacement" "$@"
  ret=$?
else 
  node  "$basedir/../webdriver-manager-replacement/bin/webdriver-manager-replacement" "$@"
  ret=$?
fi
exit $ret
